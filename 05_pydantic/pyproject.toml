[project]
name = "05-pydantic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "typing>=3.10.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
